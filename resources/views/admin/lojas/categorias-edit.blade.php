<x-admin.layouts.app title="CATEGORIAS LOJA" header="VINCULAR CATEGORIAS"><form method="POST" action="{{ route('admin.lojas.categorias.update', $loja) }}" class="bg-white border rounded p-4 space-y-2">@csrf @method('PUT') @php($vinculadas=$loja->categorias->pluck('id_categoria')->all()) @foreach($categorias as $categoria)<label class="flex gap-2"><input type="checkbox" name="categorias[]" value="{{ $categoria->id_categoria }}" @checked(in_array($categoria->id_categoria,$vinculadas))> {{ $categoria->nome }}</label>@endforeach <button class="bg-slate-900 text-white rounded px-4 py-2">SALVAR</button></form></x-admin.layouts.app>
