<div class="grid md:grid-cols-2 gap-3"><select name="id_loja" class="border rounded px-3 py-2" required>@foreach($lojas as $loja)<option value="{{ $loja->id_loja }}" @selected(old('id_loja', $oferta->id_loja ?? '')==$loja->id_loja)>{{ $loja->nome }}</option>@endforeach</select><input name="titulo" value="{{ old('titulo', $oferta->titulo ?? '') }}" class="border rounded px-3 py-2" placeholder="TITULO" required><input name="link_oferta" value="{{ old('link_oferta', $oferta->link_oferta ?? '') }}" class="border rounded px-3 py-2 md:col-span-2" placeholder="LINK OFERTA" required><input name="imagem_oferta" value="{{ old('imagem_oferta', $oferta->imagem_oferta ?? '') }}" class="border rounded px-3 py-2 md:col-span-2" placeholder="IMAGEM URL"><textarea name="descricao" class="border rounded px-3 py-2 md:col-span-2" placeholder="DESCRICAO">{{ old('descricao', $oferta->descricao ?? '') }}</textarea><input name="data_inicio" type="date" value="{{ old('data_inicio', $oferta->data_inicio ?? '') }}" class="border rounded px-3 py-2"><input name="data_expiracao" type="date" value="{{ old('data_expiracao', $oferta->data_expiracao ?? '') }}" class="border rounded px-3 py-2"><select name="status" class="border rounded px-3 py-2"><option value="1" @selected(old('status', $oferta->status ?? 1)==1)>ATIVO</option><option value="0" @selected(old('status', $oferta->status ?? 1)==0)>INATIVO</option></select></div>
