<x-admin.layouts.app title="OFERTAS" header="OFERTAS"><form class="grid md:grid-cols-5 gap-2 mb-4"><select name="id_loja" class="border rounded px-3 py-2"><option value="">LOJA</option>@foreach($lojas as $loja)<option value="{{ $loja->id_loja }}" @selected(request('id_loja')==$loja->id_loja)>{{ $loja->nome }}</option>@endforeach</select><select name="status" class="border rounded px-3 py-2"><option value="">STATUS</option><option value="1" @selected(request('status')==='1')>ATIVO</option><option value="0" @selected(request('status')==='0')>INATIVO</option></select><select name="expiracao" class="border rounded px-3 py-2"><option value="">EXPIRACAO</option><option value="hoje" @selected(request('expiracao')==='hoje')>HOJE</option><option value="7dias" @selected(request('expiracao')==='7dias')>7 DIAS</option></select><button class="bg-slate-900 text-white rounded px-4 py-2">FILTRAR</button><a href="{{ route('admin.ofertas.create') }}" class="bg-emerald-600 text-white rounded px-4 py-2 text-center">NOVA OFERTA</a></form><div class="overflow-auto bg-white border rounded"><table class="w-full text-sm"><thead><tr class="border-b"><th class="p-2 text-left">TITULO</th><th>LOJA</th><th>STATUS</th><th></th></tr></thead><tbody>@foreach($ofertas as $oferta)<tr class="border-b"><td class="p-2">{{ $oferta->titulo }}</td><td>{{ $oferta->loja->nome ?? '-' }}</td><td>{{ $oferta->status ? 'ATIVO':'INATIVO' }}</td><td class="text-right p-2"><a href="{{ route('admin.ofertas.edit',$oferta) }}">EDITAR</a><form method="POST" action="{{ route('admin.ofertas.destroy',$oferta) }}" class="inline">@csrf @method('DELETE')<button onclick="return confirm('CONFIRMAR EXCLUSAO?')">EXCLUIR</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $ofertas->links() }}</div></x-admin.layouts.app>
